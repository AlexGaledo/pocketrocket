{
  "name": "@pocketrocket/desktop",
  "version": "0.2.0",
  "private": true,
  "description": "PocketRocket desktop app: native window + managed SSH tunnel to the hub",
  "scripts": {
    "dev": "cargo tauri dev",
    "build": "echo skip",
    "build:app": "cargo tauri build",
    "test": "echo skip"
  }
}
