{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Splash page may call the connection commands",
  "windows": ["main"],
  "permissions": ["core:default"]
}
